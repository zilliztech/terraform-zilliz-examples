vpc (默认三个zone) 
storageaccount
storage identity
m identity  (操作 s id to fic；授权)
aks （操作vmss （ask rg）；init with m id ，extention 操作安装agent， 修改search vmss挂nvme ) 授权： bind to sa （storage -> index-pool,milvus-tool,maintaince -> infra-agent-sa）

privatelink
创建实例时创建 fic授权
